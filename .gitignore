# ── Node.js ──────────────────────────────────────────────────
node_modules/

# ── 실행 중 생성되는 임시 파일 ───────────────────────────────
*_tmp.json

# ── 분석 결과 엑셀 파일 ──────────────────────────────────────
*.xlsx

# ── 운영체제 잔류 파일 ───────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── 열려 있는 PPTX 임시 파일 ─────────────────────────────────
~$*.pptx

# ── Claude 로컬 설정 폴더 ────────────────────────────────────
.claude/

# ── 로컬 전용 설정 파일 ───────────────────────────────────────
settings_local.json